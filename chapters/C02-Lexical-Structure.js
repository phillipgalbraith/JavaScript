// Chapter 2 Lexical structure - lowest level syntax

// Lexical structure incldes what varriable names look like, delimiter characters for comments, 
  // separations between statements


// 2.1 The text of a JavaScript Program

//Case-sensitive, ignores line-breaks and spaces between tokens
  // Flexible formatting is easier to read.

// Recognizes/u0020 regular space character: Unicode white-space,  ASCII control characters
  // new-line, carriage return, and carriage return/line feed sequence as line terminators


// 2.2 Comments

// comment /* */ still a comment

/* 
 also a comment
   // and a comment
*/


// 2.3 Literals
// definition: a data value that appears directly in a program
// Ex:  12, 1.2, "hello world", 'hi', true, false, null


// 2.4 Identifiers and Reserved words
// Identifiers: names for - constants, variables, properties, functions, classes
  // Must start with a letter, $, _
  // May include digits after that beginning character

// Only use 'from', 'set' and 'target' from the list of words:

  // as async await break case catch class const continue debugger default delete do
  // else export extends false finally for from function get if import in instanceof 
  // let new null of return set static super switch target this throw true try 
  // typeof var void while with yield



// 2.5 Unicode

// 2.6 Optional Semicolons

// 2.7 Summary