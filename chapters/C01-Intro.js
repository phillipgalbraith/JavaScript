// 1.1 Exploring JavaScript
  // ctrl+shift+I <DevTools>
  // $ node <Node CLI>  

// 1.2 Hello World
  // to run code: node filename.js
  console.log("Hello World");

// 1.3 A Tour of JavaScript
 // objects have name-value pairs
 const book = {
    topic: 'JavaScript',
    edition: '7'
 };

  // => whatever is on this side of the error is the output/ assertion / return value
  // EXPRESSIONS return a value but do not assign a value
  // shorthand arithetic operators, // => ++ += etc
    // are STATEMENT because they assign a value
  // operands ( + = / * )
  // relational operators <,>,<=,>=,==,!==,===
  // logical operators &&, ||, !
  // a function is  parameterized block of code

  // arrow functions are anonymous and usually used as a parameter 
    //() => ;

 // for (let x of arry1){} mm

// 1.4 Example: Character Frequency Histograms


// class Classname extends Existingclass {
//   constructor(param1, param2) {
//    this.param1 = param1;
//    this.param2 = param2;
//   }

//   classMethod() {
//     
//   }
// }
// 1.5 Summary
//it's okay to skip ahead and come back
// eventually you should read it cover to cover